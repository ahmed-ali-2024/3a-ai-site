# دليل المطور الشامل - موقع 3A-AI

> **آخر تحديث:** فبراير 2026  
> **الإصدار:** 1.0  
> **الحالة:** موقع خدمات وأعمال احترافي

---

## 📋 جدول المحتويات

1. [نظرة عامة على المشروع](#نظرة-عامة-على-المشروع)
2. [البنية التقنية](#البنية-التقنية)
3. [هيكل المشروع](#هيكل-المشروع)
4. [التثبيت والإعداد](#التثبيت-والإعداد)
5. [إدارة المحتوى باستخدام Keystatic](#إدارة-المحتوى-باستخدام-keystatic)
6. [إضافة وتعديل المحتوى](#إضافة-وتعديل-المحتوى)
7. [النشر والتطوير](#النشر-والتطوير)
8. [الصيانة والتحديثات](#الصيانة-والتحديثات)
9. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة على المشروع

### ما هو هذا المشروع؟

موقع **3A-AI** هو موقع خدمات وأعمال احترافي تم بناؤه باستخدام إطار العمل **Astro**. يعرض الموقع:
- **الخدمات التقنية** التي تقدمها الشركة
- **معرض الأعمال** (Portfolio) للمشاريع المنجزة
- **صفحات ثابتة** (من نحن، اتصل بنا، سياسة الخصوصية)
- **نظام إدارة محتوى محلي** باستخدام Keystatic

### الأهداف الرئيسية


✅ **سرعة عالية**: استخدام Astro لتوليد صفحات ثابتة فائقة السرعة  
✅ **سهولة الإدارة**: لوحة تحكم محلية (Keystatic) لإدارة المحتوى بدون قاعدة بيانات  
✅ **تحسين SEO**: بنية محسّنة لمحركات البحث  
✅ **نشر تلقائي**: CI/CD عبر GitHub Actions  
✅ **تصميم متجاوب**: يعمل على جميع الأجهزة

### تاريخ المشروع

#### المرحلة 1: التأسيس (ديسمبر 2025)
- دمج مستودع المدونة مع الموقع الرئيسي
- توحيد استخدام Astro كإطار عمل أساسي
- إنشاء بنية المحتوى الأساسية

#### المرحلة 2: التحول إلى معرض أعمال (ديسمبر 2025)
- تحويل المدونة إلى معرض أعمال ومشاريع
- إضافة حقول خاصة بالمشاريع (العميل، رابط المشروع، نوع الخدمة)

#### المرحلة 3: نظام إدارة المحتوى (ديسمبر 2025)
- تجربة Decap CMS (تم الاستغناء عنه)
- الانتقال إلى **Keystatic** كنظام إدارة محتوى نهائي
- تحويل جميع الملفات من `.md` إلى `.mdoc` (Markdoc)

#### المرحلة 4: إعادة الهيكلة الكاملة (ديسمبر 2025)
- إنشاء مجموعات محتوى منفصلة: `services` و `portfolio`
- بناء صفحات الخدمات والأعمال
- تحديث التنقل والتصميم
- إعداد النشر التلقائي عبر FTP

---

## 🏗️ البنية التقنية

### التقنيات المستخدمة

| التقنية | الإصدار | الغرض |
|---------|---------|-------|
| **Astro** | 5.16.3 | إطار العمل الأساسي لبناء الموقع |
| **Keystatic** | 5.0.6 | نظام إدارة المحتوى المحلي |
| **Markdoc** | 0.15.9 | معالجة ملفات المحتوى |
| **React** | 19.2.0 | المكونات التفاعلية |
| **Tailwind CSS** | 4.1.17 | إطار عمل CSS |
| **Framer Motion** | 12.23.24 | الحركات والأنيميشن |
| **Vitest** | 4.0.18 | اختبار الوحدات |
| **TypeScript** | - | لغة البرمجة |

### معمارية المشروع

```
┌─────────────────────────────────────────┐
│         GitHub Repository               │
│  (الكود المصدري + المحتوى)              │
└──────────────┬──────────────────────────┘
               │
               │ Push to main branch
               ▼
┌─────────────────────────────────────────┐
│       GitHub Actions (CI/CD)            │
│  - npm install                          │
│  - npm run build                        │
│  - Deploy via FTP                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      Production Server (FTP)            │
│  - Static HTML/CSS/JS files             │
│  - Images & Assets                      │
└─────────────────────────────────────────┘
```

### نموذج البيانات

المشروع يستخدم **Flat File System** (ملفات نصية) بدلاً من قاعدة بيانات:


- **المحتوى**: ملفات `.mdoc` في `src/content/`
- **الإعدادات**: ملفات `.json` في `src/config/` و `src/data/`
- **الصور**: ملفات في `public/images/`

---

## 📁 هيكل المشروع

### الهيكل العام

```
3aai-site/
├── .github/
│   └── workflows/
│       └── deploy.yml          # إعدادات النشر التلقائي
├── docs/                       # التوثيق
│   ├── DEVELOPER_GUIDE.md      # هذا الملف
│   ├── TODO.md                 # المهام المستقبلية
│   └── session-2025-12-06.md  # سجل الجلسات
├── internal_docs/              # وثائق داخلية
│   └── project_history.md      # تاريخ المشروع
├── public/                     # الملفات الثابتة
│   ├── images/                 # الصور
│   │   ├── blog/
│   │   ├── portfolio/
│   │   ├── services/
│   │   └── pages/
│   ├── .htaccess              # إعدادات Apache
│   ├── logo.png
│   └── favicon.svg
├── src/
│   ├── components/            # المكونات
│   │   ├── blog/             # مكونات المدونة
│   │   ├── Footer.tsx
│   │   ├── Hero.tsx
│   │   ├── Navbar.tsx
│   │   └── ...
│   ├── config/               # ملفات الإعدادات
│   │   ├── site.json        # إعدادات الموقع
│   │   ├── site.ts
│   │   └── theme.ts
│   ├── content/              # المحتوى (ملفات .mdoc)
│   │   ├── blog/            # المقالات
│   │   ├── portfolio/       # معرض الأعمال
│   │   ├── services/        # الخدمات
│   │   ├── pages/           # الصفحات الثابتة
│   │   ├── authors/         # المؤلفون
│   │   └── config.ts        # تعريف schemas
│   ├── data/                # البيانات الثابتة
│   │   ├── navigation.json  # قوائم التنقل
│   │   └── social.json      # روابط التواصل
│   ├── layouts/             # قوالب الصفحات
│   │   ├── Layout.astro
│   │   └── blog/
│   ├── pages/               # صفحات الموقع
│   │   ├── index.astro      # الصفحة الرئيسية
│   │   ├── services/        # صفحات الخدمات
│   │   ├── portfolio/       # صفحات الأعمال
│   │   ├── about.astro
│   │   ├── contact.astro
│   │   └── ...
│   ├── styles/              # ملفات CSS
│   │   ├── global.css
│   │   └── blog-global.css
│   └── utils/               # دوال مساعدة
│       ├── date.ts
│       ├── image.ts
│       └── ...
├── astro.config.mjs         # إعدادات Astro
├── keystatic.config.ts      # إعدادات Keystatic
├── package.json             # التبعيات والأوامر
├── tailwind.config.js       # إعدادات Tailwind
└── tsconfig.json            # إعدادات TypeScript
```


### الملفات الرئيسية

#### 1. `astro.config.mjs`
ملف إعدادات Astro الرئيسي:
```javascript
export default defineConfig({
  site: 'http://localhost:4321',
  integrations: [
    react(),
    markdoc(),
    // Keystatic يعمل فقط في وضع التطوير
    ...(process.env.NODE_ENV !== 'production' ? [keystatic()] : []),
  ]
});
```

**ملاحظة مهمة**: Keystatic معطّل في الإنتاج لأنه نظام محلي فقط.

#### 2. `keystatic.config.ts`
ملف إعدادات لوحة التحكم - يحدد:
- **المجموعات** (Collections): blog, portfolio, services, pages, authors
- **الحقول** (Fields): العناوين، الأوصاف، الصور، التواريخ، إلخ
- **التخزين**: محلي (local storage)

#### 3. `src/content/config.ts`
تعريف schemas لـ Astro Content Collections:
- التحقق من صحة البيانات (Validation)
- تحديد أنواع الحقول (Types)

#### 4. `.github/workflows/deploy.yml`
سكريبت النشر التلقائي:
1. تثبيت Node.js
2. تثبيت التبعيات (`npm ci`)
3. بناء المشروع (`npm run build`)
4. رفع الملفات عبر FTP

---

## 🚀 التثبيت والإعداد

### المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:
- **Node.js** (الإصدار 20 أو أحدث)
- **npm** (يأتي مع Node.js)
- **Git** (لإدارة الإصدارات)
- محرر نصوص (VS Code موصى به)

### خطوات التثبيت

#### 1. استنساخ المستودع

```bash
git clone https://github.com/your-username/3aai-site.git
cd 3aai-site
```

#### 2. تثبيت التبعيات

```bash
npm install
```

هذا الأمر سيقوم بتثبيت جميع المكتبات المطلوبة المذكورة في `package.json`.

#### 3. تشغيل الموقع محلياً

```bash
npm run dev
```

سيعمل الموقع على: `http://localhost:4321`

#### 4. الوصول إلى لوحة التحكم

افتح المتصفح وانتقل إلى:
```
http://localhost:4321/keystatic
```

**ملاحظة**: لوحة التحكم تعمل فقط في وضع التطوير (`npm run dev`).

### الأوامر المتاحة

| الأمر | الوظيفة |
|-------|---------|
| `npm run dev` | تشغيل الموقع في وضع التطوير |
| `npm run build` | بناء الموقع للإنتاج |
| `npm run preview` | معاينة النسخة المبنية |
| `npm test` | تشغيل الاختبارات |
| `npm run test:ui` | تشغيل واجهة الاختبارات |

---

## 🎨 إدارة المحتوى باستخدام Keystatic

### ما هو Keystatic؟

**Keystatic** هو نظام إدارة محتوى (CMS) محلي يعمل مباشرة في بيئة التطوير. يتيح لك:
- ✅ إضافة وتعديل المحتوى عبر واجهة مرئية
- ✅ رفع الصور بسهولة
- ✅ معاينة التغييرات فوراً
- ✅ حفظ التغييرات في Git تلقائياً

### الوصول إلى لوحة التحكم

1. شغّل الموقع في وضع التطوير:
   ```bash
   npm run dev
   ```

2. افتح المتصفح على:
   ```
   http://localhost:4321/keystatic
   ```

3. ستظهر لك الواجهة الرئيسية مع القوائم التالية:
   - **إعدادات الموقع** (Singleton)
   - **المقالات والمشاريع** (Blog)
   - **الصفحات الثابتة** (Pages)
   - **المؤلفون** (Authors)
   - **الخدمات** (Services)
   - **معرض الأعمال** (Portfolio)

### واجهة لوحة التحكم

#### القائمة الجانبية
```
┌─────────────────────────┐
│ 📋 إعدادات الموقع      │
├─────────────────────────┤
│ 📝 المقالات والمشاريع  │
│ 📄 الصفحات الثابتة     │
│ 👤 المؤلفون            │
│ 🛠️ الخدمات             │
│ 💼 معرض الأعمال        │
└─────────────────────────┘
```

#### أزرار التحكم
- **Create Entry**: إنشاء محتوى جديد
- **Edit**: تعديل محتوى موجود
- **Delete**: حذف محتوى
- **Save**: حفظ التغييرات

---

## ✏️ إضافة وتعديل المحتوى

### 1️⃣ إضافة خدمة جديدة

#### الخطوات:

1. **افتح لوحة التحكم**
   - انتقل إلى `http://localhost:4321/keystatic`
   - اضغط على **"الخدمات"** من القائمة الجانبية

2. **إنشاء خدمة جديدة**
   - اضغط على زر **"Create Service"**
   - ستظهر لك نموذج بالحقول التالية:

#### الحقول المطلوبة:

| الحقل | الوصف | مثال |
|-------|-------|------|
| **الرابط (Slug)** | معرّف فريد للخدمة في الرابط | `ai-solutions` |
| **عنوان الخدمة** | الاسم الظاهر للخدمة | `حلول الذكاء الاصطناعي` |
| **وصف الخدمة** | وصف مختصر (2-3 أسطر) | `نقدم حلول ذكاء اصطناعي متقدمة...` |
| **أيقونة الخدمة** | رمز تعبيري (Emoji) | `🤖` أو `💻` |
| **صورة الخدمة** | صورة توضيحية | رفع صورة من جهازك |
| **مميزات الخدمة** | قائمة بالمميزات | `- تطوير نماذج AI` |
| **ترتيب العرض** | رقم لترتيب الخدمات | `1`, `2`, `3` |
| **مسودة؟** | إخفاء الخدمة من الموقع | ☐ (غير محدد) |
| **تفاصيل الخدمة** | محتوى تفصيلي بصيغة Markdown | نص طويل مع تنسيق |

#### مثال عملي:

```yaml
الرابط: backend-as-a-service
عنوان الخدمة: Backend as a Service (BaaS)
وصف الخدمة: نوفر بنية خلفية جاهزة وقابلة للتوسع لتطبيقاتك
أيقونة الخدمة: ☁️
ترتيب العرض: 2
مسودة: ☐
```

**مميزات الخدمة:**
```
- قواعد بيانات سحابية
- API جاهزة
- مصادقة المستخدمين
- تخزين الملفات
```

**تفاصيل الخدمة:**
```markdown
## ما هو BaaS؟

Backend as a Service يوفر لك بنية خلفية كاملة بدون الحاجة لبناء سيرفر خاص.

## المميزات

- سرعة في التطوير
- تكلفة أقل
- قابلية للتوسع
```

3. **رفع الصورة**
   - اضغط على حقل "صورة الخدمة"
   - اختر صورة من جهازك (يفضل بصيغة `.webp` أو `.jpg`)
   - سيتم رفعها تلقائياً إلى `public/images/services/`

4. **حفظ الخدمة**
   - اضغط على زر **"Save"** أعلى الصفحة
   - سيتم إنشاء ملف جديد في `src/content/services/`

5. **معاينة النتيجة**
   - افتح `http://localhost:4321/services`
   - ستجد الخدمة الجديدة ظاهرة

---

### 2️⃣ إضافة مشروع في معرض الأعمال

#### الخطوات:

1. **افتح قسم معرض الأعمال**
   - من لوحة التحكم، اضغط على **"معرض الأعمال"**
   - اضغط على **"Create Portfolio"**

#### الحقول المطلوبة:

| الحقل | الوصف | مثال |
|-------|-------|------|
| **الرابط (Slug)** | معرّف فريد للمشروع | `my-awesome-project` |
| **عنوان المشروع** | اسم المشروع | `تطبيق CookIt` |
| **وصف المشروع** | وصف مختصر | `تطبيق ذكي للوصفات...` |
| **اسم العميل** | اسم العميل أو الجهة | `شركة XYZ` |
| **تاريخ الإنجاز** | تاريخ إتمام المشروع | `2024-11-01` |
| **الصورة الرئيسية** | صورة غلاف المشروع | رفع صورة |
| **تصنيف المشروع** | نوع المشروع | `تطبيقات موبايل` |
| **التقنيات المستخدمة** | قائمة التقنيات | `Flutter`, `Firebase` |
| **رابط المشروع** | رابط المشروع الحي | `https://example.com` |
| **مشروع مميز؟** | عرض المشروع كمميز | ☑ (محدد) |
| **مسودة؟** | إخفاء المشروع | ☐ |
| **تفاصيل المشروع** | محتوى تفصيلي | نص طويل |

#### مثال عملي:

```yaml
الرابط: cookit-app
عنوان المشروع: منصة CookIt - الذكاء الاصطناعي في خدمة الطهي
وصف المشروع: تطبيق ذكي يساعد المستخدمين على اكتشاف وصفات جديدة
اسم العميل: مشروع داخلي 3A-AI
تاريخ الإنجاز: 2024-11-01
تصنيف المشروع: تطبيقات موبايل
رابط المشروع: https://cookit.3aai.in/
مشروع مميز: ☑
```

**التقنيات المستخدمة:**
```
- Flutter
- Cross-Platform Development
- AI Integration
- BaaS
```

2. **حفظ ومعاينة**
   - اضغط **"Save"**
   - افتح `http://localhost:4321/portfolio`
   - ستجد المشروع في معرض الأعمال

---

### 3️⃣ تعديل إعدادات الموقع

#### الخطوات:

1. **افتح إعدادات الموقع**
   - من لوحة التحكم، اضغط على **"إعدادات الموقع"**
   - هذا قسم **Singleton** (إعدادات واحدة للموقع كله)

#### الحقول المتاحة:

##### معلومات أساسية
- **عنوان الموقع**: `3AAI`
- **وصف الموقع**: `الموقع الرسمي لـ 3AAI`
- **رابط الموقع**: `https://3aai.in`

##### معلومات الكاتب
- **اسم الكاتب**: `3AAI`
- **نبذة عن الكاتب**: نص قصير
- **الصورة الشخصية**: مسار الصورة
- **البريد الإلكتروني**: `support@3aai.in`

##### الشعار
- **مسار الشعار**: `/logo.png`
- **النص البديل للشعار**: `3AAI`
- **نص الشعار**: `3AAI`

##### روابط التواصل الاجتماعي
يمكنك إضافة عدة روابط:
```
المنصة: facebook
الرابط: https://www.facebook.com/3a4ai/
الأيقونة: facebook
```

##### قائمة التنقل
قائمة بعناصر القائمة الرئيسية (حالياً تُدار من `src/data/navigation.json`)

##### إعدادات الفوتر
- **قسم من نحن**: عنوان ووصف
- **روابط الفوتر**: قائمة روابط
- **حقوق النشر**: نص حقوق النشر

##### إعدادات أخرى
- **عدد المقالات في الصفحة**: `10`
- **اللغة**: `ar`
- **الاتجاه**: `rtl`

2. **حفظ التغييرات**
   - بعد التعديل، اضغط **"Save"**
   - سيتم تحديث ملف `src/config/site.json`

---

### 4️⃣ إضافة صفحة ثابتة

#### الخطوات:

1. **افتح قسم الصفحات الثابتة**
   - اضغط على **"الصفحات الثابتة"**
   - اضغط **"Create Page"**

#### الحقول:

| الحقل | الوصف |
|-------|-------|
| **الرابط (Slug)** | معرّف الصفحة (مثل: `about`, `contact`) |
| **العنوان** | عنوان الصفحة |
| **الوصف** | وصف مختصر للصفحة |
| **صورة الغلاف** | صورة اختيارية |
| **المحتوى** | محتوى الصفحة بصيغة Markdown |

#### مثال:

```yaml
الرابط: about
العنوان: من نحن
الوصف: تعرف على فريق 3A-AI ورؤيتنا
```

**المحتوى:**
```markdown
## من نحن

نحن فريق **3A-AI** المتخصص في تقديم حلول تقنية متقدمة.

### رؤيتنا

بناء المستقبل الرقمي باستخدام أحدث التقنيات.

### خدماتنا

- تطوير الويب
- تطبيقات الموبايل
- حلول الذكاء الاصطناعي
```

---

### 5️⃣ إضافة مؤلف جديد

#### الخطوات:

1. **افتح قسم المؤلفون**
   - اضغط على **"المؤلفون"**
   - اضغط **"Create Author"**

#### الحقول:

| الحقل | الوصف |
|-------|-------|
| **الرابط (Slug)** | معرّف المؤلف |
| **الاسم** | اسم المؤلف |
| **الوظيفة/الدور** | مثل: `مطور ويب`, `مصمم` |
| **الصورة الشخصية** | صورة المؤلف |
| **النبذة المختصرة** | نص قصير عن المؤلف |
| **روابط التواصل** | Twitter, GitHub, LinkedIn |
| **السيرة الذاتية الكاملة** | محتوى تفصيلي |

---

### 6️⃣ إدارة الصور

#### رفع الصور عبر Keystatic

عند رفع صورة من خلال لوحة التحكم:

1. **اضغط على حقل الصورة** في أي نموذج
2. **اختر "Upload Image"**
3. **حدد الصورة من جهازك**
4. سيتم رفعها تلقائياً إلى المجلد المناسب:
   - خدمات → `public/images/services/`
   - أعمال → `public/images/portfolio/`
   - مقالات → `public/images/blog/`
   - صفحات → `public/images/pages/`

#### أفضل الممارسات للصور:

✅ **الصيغة**: استخدم `.webp` للحجم الأصغر، أو `.jpg` للتوافق  
✅ **الحجم**: لا تزيد عن 1MB للصورة الواحدة  
✅ **الأبعاد**: 
   - صور الخدمات: `800x600` بكسل
   - صور الأعمال: `1200x800` بكسل
   - صور المقالات: `1200x630` بكسل (مناسب لمشاركة السوشيال ميديا)

✅ **التسمية**: استخدم أسماء وصفية بالإنجليزية:
   - ❌ `صورة1.jpg`
   - ✅ `web-development-service.webp`

#### رفع الصور يدوياً

إذا أردت رفع الصور مباشرة بدون لوحة التحكم:

1. ضع الصورة في المجلد المناسب في `public/images/`
2. استخدم المسار في ملف `.mdoc`:
   ```yaml
   image: /images/services/my-service/image.webp
   ```

---

### 7️⃣ تعديل قوائم التنقل

#### تعديل قائمة الهيدر والفوتر

القوائم تُدار من ملف `src/data/navigation.json`:

```json
{
    "header": [
        {
            "text": "الرئيسية",
            "href": "/"
        },
        {
            "text": "الخدمات",
            "href": "/services"
        },
        {
            "text": "أعمالنا",
            "href": "/portfolio"
        }
    ],
    "footer": [
        // نفس الهيكل
    ]
}
```

#### إضافة عنصر جديد:

1. افتح `src/data/navigation.json`
2. أضف عنصر جديد:
   ```json
   {
       "text": "المدونة",
       "href": "/blog"
   }
   ```
3. احفظ الملف
4. سيظهر العنصر تلقائياً في القائمة

---

### 8️⃣ حفظ التغييرات في Git

بعد إجراء أي تعديلات عبر Keystatic أو يدوياً:

```bash
# 1. تحقق من الملفات المعدلة
git status

# 2. أضف جميع التغييرات
git add .

# 3. اكتب رسالة توضيحية
git commit -m "إضافة خدمة جديدة: حلول الذكاء الاصطناعي"

# 4. ارفع التغييرات إلى GitHub
git push origin main
```

**ملاحظة**: عند الرفع إلى `main`، سيتم تشغيل النشر التلقائي عبر GitHub Actions.

---

## 🚢 النشر والتطوير

### بيئات العمل

المشروع يعمل في بيئتين:

#### 1. بيئة التطوير (Development)
- **الأمر**: `npm run dev`
- **الرابط**: `http://localhost:4321`
- **المميزات**:
  - Hot Reload (إعادة تحميل تلقائية)
  - لوحة تحكم Keystatic متاحة
  - رسائل خطأ تفصيلية

#### 2. بيئة الإنتاج (Production)
- **الأمر**: `npm run build`
- **المخرجات**: مجلد `dist/`
- **المميزات**:
  - ملفات HTML ثابتة محسّنة
  - CSS و JS مضغوطة
  - صور محسّنة
  - لا توجد لوحة تحكم

---

### النشر التلقائي عبر GitHub Actions

#### كيف يعمل؟

```
┌──────────────────────────────────────┐
│  1. تعديل المحتوى محلياً            │
│     (عبر Keystatic أو يدوياً)        │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  2. حفظ التغييرات في Git             │
│     git add . && git commit          │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  3. رفع إلى GitHub                   │
│     git push origin main             │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  4. GitHub Actions يبدأ العمل       │
│     - تثبيت Node.js                  │
│     - تثبيت التبعيات (npm ci)        │
│     - بناء المشروع (npm run build)   │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  5. رفع الملفات عبر FTP              │
│     من dist/ إلى السيرفر             │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  6. الموقع متاح على الإنترنت! 🎉    │
└──────────────────────────────────────┘
```

#### إعداد GitHub Secrets

لكي يعمل النشر التلقائي، يجب إضافة المتغيرات التالية في GitHub:

1. انتقل إلى **Settings** → **Secrets and variables** → **Actions**
2. أضف المتغيرات التالية:

| المتغير | الوصف |
|---------|-------|
| `FTP_SERVER` | عنوان السيرفر (مثل: `ftp.example.com`) |
| `FTP_USERNAME` | اسم المستخدم FTP |
| `FTP_PASSWORD` | كلمة المرور FTP |

---

### النشر اليدوي

إذا أردت النشر يدوياً بدون GitHub Actions:

#### 1. بناء المشروع

```bash
npm run build
```

سيتم إنشاء مجلد `dist/` يحتوي على الملفات الجاهزة.

#### 2. رفع الملفات

استخدم أي برنامج FTP مثل:
- **FileZilla** (مجاني)
- **Cyberduck** (Mac)
- **WinSCP** (Windows)

**الخطوات:**
1. اتصل بالسيرفر باستخدام بيانات FTP
2. ارفع محتويات مجلد `dist/` إلى المجلد الرئيسي للموقع
3. تأكد من رفع ملف `.htaccess` أيضاً

---

### معاينة النسخة المبنية محلياً

قبل النشر، يمكنك معاينة النسخة المبنية:

```bash
# 1. بناء المشروع
npm run build

# 2. معاينة النسخة المبنية
npm run preview
```

سيعمل الموقع على `http://localhost:4321` بنفس شكل الإنتاج.

---

## 🔧 الصيانة والتحديثات

### تحديث التبعيات

#### فحص التحديثات المتاحة

```bash
npm outdated
```

سيعرض قائمة بالمكتبات التي لها إصدارات أحدث.

#### تحديث جميع التبعيات

```bash
# تحديث التبعيات الصغيرة (Patch & Minor)
npm update

# تحديث التبعيات الرئيسية (Major) - بحذر!
npm install <package-name>@latest
```

#### تحديث Astro

```bash
npm install astro@latest
```

**ملاحظة**: بعد تحديث Astro، تحقق من [سجل التغييرات](https://github.com/withastro/astro/releases) للتأكد من عدم وجود تغييرات كبيرة.

---

### النسخ الاحتياطي

#### 1. نسخ احتياطي للمحتوى

المحتوى موجود في `src/content/` ومحفوظ في Git تلقائياً.

للحصول على نسخة احتياطية:
```bash
# نسخ مجلد المحتوى
cp -r src/content/ backup/content-$(date +%Y%m%d)/
```

#### 2. نسخ احتياطي للصور

```bash
# نسخ مجلد الصور
cp -r public/images/ backup/images-$(date +%Y%m%d)/
```

#### 3. نسخ احتياطي كامل

```bash
# إنشاء أرشيف مضغوط
tar -czf backup-$(date +%Y%m%d).tar.gz \
  src/content/ \
  public/images/ \
  src/config/ \
  src/data/
```

---

### إضافة ميزات جديدة

#### إضافة مجموعة محتوى جديدة

إذا أردت إضافة نوع محتوى جديد (مثل: "الشهادات" أو "الفريق"):

1. **تحديث `src/content/config.ts`**:
   ```typescript
   const testimonialsCollection = defineCollection({
     type: 'content',
     schema: z.object({
       name: z.string(),
       role: z.string(),
       content: z.string(),
       rating: z.number().min(1).max(5),
     }),
   });

   export const collections = {
     // ... المجموعات الموجودة
     testimonials: testimonialsCollection,
   };
   ```

2. **تحديث `keystatic.config.ts`**:
   ```typescript
   collections: {
     // ... المجموعات الموجودة
     testimonials: collection({
       label: 'الشهادات',
       slugField: 'slug',
       path: 'src/content/testimonials/*',
       schema: {
         slug: fields.slug({ name: { label: 'الرابط' } }),
         name: fields.text({ label: 'الاسم' }),
         role: fields.text({ label: 'الوظيفة' }),
         // ... باقي الحقول
       },
     }),
   }
   ```

3. **إنشاء صفحة العرض** في `src/pages/testimonials/index.astro`

---

## 🐛 استكشاف الأخطاء

### المشاكل الشائعة وحلولها

#### 1. لوحة التحكم لا تظهر

**المشكلة**: عند فتح `/keystatic` لا تظهر اللوحة

**الحلول**:
```bash
# تأكد من أنك في وضع التطوير
npm run dev

# تحقق من أن Keystatic مثبت
npm list @keystatic/astro

# أعد تثبيت التبعيات
rm -rf node_modules package-lock.json
npm install
```

---

#### 2. الصور لا تظهر

**المشكلة**: الصور المرفوعة لا تظهر في الموقع

**الحلول**:
- تحقق من مسار الصورة في ملف `.mdoc`:
  ```yaml
  # ✅ صحيح
  image: /images/services/my-service/image.webp
  
  # ❌ خطأ
  image: images/services/my-service/image.webp
  ```

- تأكد من وجود الصورة في `public/images/`

- تحقق من صيغة الصورة (يفضل `.webp` أو `.jpg`)

---

#### 3. خطأ في البناء (Build Error)

**المشكلة**: `npm run build` يفشل

**الحلول**:
```bash
# 1. تحقق من الأخطاء في الكونسول
npm run build

# 2. تحقق من صحة ملفات المحتوى
# تأكد من أن جميع الحقول المطلوبة موجودة

# 3. تحقق من صحة TypeScript
npx astro check

# 4. امسح الكاش وأعد البناء
rm -rf .astro dist
npm run build
```

---

#### 4. التغييرات لا تظهر

**المشكلة**: بعد التعديل، التغييرات لا تظهر في المتصفح

**الحلول**:
```bash
# 1. امسح كاش المتصفح (Ctrl+Shift+R أو Cmd+Shift+R)

# 2. أعد تشغيل السيرفر
# اضغط Ctrl+C لإيقاف السيرفر
npm run dev

# 3. امسح كاش Astro
rm -rf .astro
npm run dev
```

---

#### 5. خطأ في Git Push

**المشكلة**: لا يمكن رفع التغييرات إلى GitHub

**الحلول**:
```bash
# 1. تحقق من حالة Git
git status

# 2. تحقق من الفرع الحالي
git branch

# 3. تأكد من أنك على فرع main
git checkout main

# 4. اسحب آخر التحديثات أولاً
git pull origin main

# 5. ثم ارفع تغييراتك
git push origin main
```

---

#### 6. GitHub Actions فشل

**المشكلة**: النشر التلقائي فشل

**الحلول**:
1. افتح GitHub → **Actions** → اضغط على آخر Workflow
2. اقرأ رسالة الخطأ
3. الأخطاء الشائعة:
   - **بيانات FTP خاطئة**: تحقق من Secrets
   - **خطأ في البناء**: راجع قسم "خطأ في البناء" أعلاه
   - **مهلة الاتصال**: تحقق من اتصال السيرفر

---

### رسائل الخطأ الشائعة

#### `Cannot find module '@astrojs/...'`

**الحل**:
```bash
npm install
```

---

#### `[ERROR] Could not resolve "..."`

**الحل**:
```bash
# امسح node_modules وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

---

#### `Port 4321 is already in use`

**الحل**:
```bash
# أوقف العملية التي تستخدم المنفذ
# Linux/Mac:
lsof -ti:4321 | xargs kill -9

# أو استخدم منفذ آخر
npm run dev -- --port 3000
```

---

## 📚 موارد إضافية

### الوثائق الرسمية

- **Astro**: https://docs.astro.build
- **Keystatic**: https://keystatic.com/docs
- **Markdoc**: https://markdoc.dev
- **Tailwind CSS**: https://tailwindcss.com/docs

### الدعم والمساعدة

- **Discord Astro**: https://astro.build/chat
- **GitHub Issues**: افتح issue في المستودع
- **Stack Overflow**: ابحث عن `[astro]` أو `[keystatic]`

---

## 🎓 نصائح للمطورين

### أفضل الممارسات

#### 1. التزم بالهيكل الموجود
- لا تغير أسماء المجلدات الرئيسية
- اتبع نفس نمط التسمية للملفات

#### 2. اكتب تعليقات واضحة
```typescript
// ✅ جيد
// جلب جميع الخدمات المنشورة وترتيبها حسب الأولوية
const services = await getCollection("services");

// ❌ سيء
// get services
const services = await getCollection("services");
```

#### 3. اختبر قبل النشر
```bash
# دائماً اختبر البناء محلياً قبل الرفع
npm run build
npm run preview
```

#### 4. استخدم Git بشكل صحيح
```bash
# رسائل commit واضحة
git commit -m "إضافة خدمة: تطوير تطبيقات الموبايل"

# وليس
git commit -m "update"
```

#### 5. احتفظ بنسخ احتياطية
- اعمل نسخة احتياطية قبل التحديثات الكبيرة
- استخدم Git branches للميزات الجديدة

---

### سير العمل الموصى به

#### للتعديلات الصغيرة (محتوى فقط)

```bash
1. npm run dev                    # شغّل السيرفر
2. افتح /keystatic               # عدّل المحتوى
3. git add .                      # أضف التغييرات
4. git commit -m "..."            # احفظ
5. git push origin main           # انشر
```

#### للتعديلات الكبيرة (كود + محتوى)

```bash
1. git checkout -b feature/new-feature    # فرع جديد
2. npm run dev                            # شغّل السيرفر
3. [عدّل الكود والمحتوى]
4. npm run build                          # اختبر البناء
5. npm run preview                        # عاين النتيجة
6. git add .
7. git commit -m "..."
8. git push origin feature/new-feature
9. [افتح Pull Request في GitHub]
10. [بعد المراجعة] Merge إلى main
```

---

## 📋 قائمة مرجعية للنشر

قبل نشر أي تحديث، تأكد من:

- [ ] جميع الصور محسّنة وبحجم مناسب
- [ ] لا توجد روابط مكسورة
- [ ] جميع الحقول المطلوبة مملوءة
- [ ] لا توجد مسودات منشورة بالخطأ
- [ ] `npm run build` يعمل بدون أخطاء
- [ ] `npm run preview` يعرض الموقع بشكل صحيح
- [ ] رسالة commit واضحة ووصفية
- [ ] تم اختبار الموقع على الموبايل (Responsive)

---

## 🔐 الأمان

### حماية بيانات FTP

- **لا تشارك** بيانات FTP في الكود
- استخدم **GitHub Secrets** فقط
- غيّر كلمة المرور بشكل دوري

### حماية الموقع

ملف `.htaccess` يحتوي على:
- حماية من XSS
- HTTPS Redirect
- Compression
- Caching Headers

---

## 📞 الدعم الفني

إذا واجهت مشكلة لا تستطيع حلها:

1. **راجع هذا الدليل** أولاً
2. **ابحث في Issues** على GitHub
3. **اسأل في Discord** الخاص بـ Astro
4. **افتح Issue جديد** في المستودع

---

## 📝 ملاحظات ختامية

### ما تم إنجازه

✅ موقع سريع وحديث باستخدام Astro  
✅ نظام إدارة محتوى سهل (Keystatic)  
✅ نشر تلقائي عبر GitHub Actions  
✅ تصميم متجاوب ومحسّن لـ SEO  
✅ بنية قابلة للتوسع

### المهام المستقبلية

راجع ملف `docs/TODO.md` للمهام المقترحة.

---

## 🎉 الخلاصة

هذا الدليل يغطي كل ما تحتاجه للعمل على المشروع:
- **الفهم**: بنية المشروع والتقنيات المستخدمة
- **الإدارة**: إضافة وتعديل المحتوى عبر Keystatic
- **النشر**: رفع التحديثات تلقائياً
- **الصيانة**: حل المشاكل والتحديثات

**نصيحة أخيرة**: احتفظ بهذا الدليل مرجعاً دائماً، وحدّثه عند إضافة ميزات جديدة.

---

**تم التوثيق بواسطة**: Kiro AI  
**التاريخ**: فبراير 2026  
**الإصدار**: 1.0

