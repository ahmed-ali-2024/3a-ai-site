# المرجع السريع - موقع 3A-AI

> دليل مختصر للعمليات الشائعة

---

## 🚀 البدء السريع

```bash
# تثبيت التبعيات
npm install

# تشغيل الموقع
npm run dev

# فتح لوحة التحكم
# افتح: http://localhost:4321/keystatic
```

---

## 📝 إضافة محتوى جديد

### خدمة جديدة
1. افتح `/keystatic`
2. اضغط "الخدمات" → "Create Service"
3. املأ الحقول
4. احفظ

### مشروع جديد
1. افتح `/keystatic`
2. اضغط "معرض الأعمال" → "Create Portfolio"
3. املأ الحقول
4. احفظ

---

## 💾 حفظ ونشر

```bash
# حفظ التغييرات
git add .
git commit -m "وصف التغيير"
git push origin main
```

**النشر التلقائي سيبدأ فوراً!**

---

## 🖼️ رفع الصور

### عبر Keystatic
- اضغط على حقل الصورة
- اختر "Upload Image"
- حدد الصورة

### يدوياً
- ضع الصورة في `public/images/[القسم]/`
- استخدم المسار: `/images/[القسم]/اسم-الصورة.webp`

---

## 🔧 الأوامر الأساسية

| الأمر | الوظيفة |
|-------|---------|
| `npm run dev` | تشغيل وضع التطوير |
| `npm run build` | بناء للإنتاج |
| `npm run preview` | معاينة النسخة المبنية |
| `npm test` | تشغيل الاختبارات |

---

## 📁 المجلدات المهمة

```
src/content/          # المحتوى (.mdoc)
├── services/         # الخدمات
├── portfolio/        # الأعمال
├── blog/            # المقالات
└── pages/           # الصفحات

public/images/       # الصور
├── services/
├── portfolio/
└── blog/

src/data/            # البيانات
└── navigation.json  # القوائم
```

---

## 🐛 حل المشاكل السريع

### لوحة التحكم لا تعمل
```bash
npm run dev
# تأكد من فتح /keystatic
```

### الصور لا تظهر
- تحقق من المسار: `/images/...`
- تأكد من وجود الصورة في `public/`

### خطأ في البناء
```bash
rm -rf .astro dist node_modules
npm install
npm run build
```

---

## 📞 روابط مفيدة

- **الموقع المحلي**: http://localhost:4321
- **لوحة التحكم**: http://localhost:4321/keystatic
- **الدليل الكامل**: `docs/DEVELOPER_GUIDE.md`

---

**للمزيد من التفاصيل، راجع الدليل الشامل في `DEVELOPER_GUIDE.md`**
